# Day 002. 사용자의 소중한 비밀번호 암호화 (Spring Security 기초)
- 사용자가 입력한 비밀번호를 그대로 DB에 저장(Plain Text)하면 어떻게 될까?
- 해커가 DB를 털어가거나, 심지어 개발자들이 DB를 열어봤을 때 모든 사람의 비밀번호를 볼 수 있다면 엄청난 보안 사고 이어짐
- 따라서 실무에서는 단방향 해시 암호화(BCrypt)를 

##

### Git 작업장 생성
```bash
git switch develop
git checkout -b feature/security-init
```

### 